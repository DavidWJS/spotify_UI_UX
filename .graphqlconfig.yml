projects:
  spotify:
    schemaPath: amplify/backend/api/spotify/build/schema.graphql
    includes:
      - src/graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/API.ts
        docsFilePath: /graphql
extensions:
  amplify:
    version: 3
